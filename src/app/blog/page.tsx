import React from 'react';
import styles from './Blog.module.css';

const Blog = () => {
  return (
    <div className={styles.wrapper}>
      <section>
        <h1>Blog</h1>
      </section>
    </div>
  );
};

export default Blog;
